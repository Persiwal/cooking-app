import '@testing-library/jest-dom';
// import { mockRegisterFormProps } from './RegisterForm.mocks.ts';

describe('Base client template component', () => {
  it('Should render properly', () => {
    // render(<RegisterForm {...mockRegisterFormProps.base} />);
    // const div = screen.getByText(mockRegisterFormProps.base.sampleTextProp);
    // expect(div).toHaveTextContent(mockRegisterFormProps.base.sampleTextProp);
  });
});
